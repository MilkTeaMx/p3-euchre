{
    "configurations": [
    {
        "type": "lldb",
        "request": "launch",
        "name": "Launch",
        "program": "${workspaceFolder}/Card_public_tests.exe",
        "args": [],
        "cwd": "${workspaceFolder}"
    }
    ]
}